<!-- <view class="page-wrap"> 
  <scroll-view class="container" scroll-y="true">
     <view class="post-list"> 
        <view class="post-card" wx:for="{{documents}}" wx:for-item="document" wx:for-index="document_index">
          <view class="card-header flex flex-row">

               <view class="post-name flex-stretch" bindtap = "showDocument" data-path = "{{document.url}}">{{document.title}}</view> 
            <!-- <view class="post-time">{{document.createdAt}}</view>  -->
          <!-- </view>
          

        </view>
     </view> 
 </scroll-view>
</view> -->


<view class="container blog-view">
<!-- <image src="{{imgUrl}}" class="banner"/> -->

<view onReachBottom="getData">
  <view class="article-item" wx:for="{{ documents }}"   wx:for-item="document" wx:for-index="idx" bindtap="showDocument" 
  data-path = "{{document.content_url}}" data-document="{{document}}" >
      <text class="title">{{ document.title }}</text>
      <view class="desc-with-pic" wx:if="{{ document.cover }}">
      <block wx:if="{{document.content}}">
      {{ document.content }}
      </block>
     <text wx:else>\n\n\n\n</text>
      <text class="sort">{{ document.datetime}}</text>
      <!-- testtesttesttesttesttesttestt
      esttesttesttesttesttesttest
      esttesttesttesttesttesttest -->
        
        <image src="{{ document.cover }}" class="desc-img"></image>
      </view>
      <!-- <view class="desc-with-pics" wx:if="{{ item.img.length === 0 || item.img.length > 1 }}">
        {{ document.excerpt }} -->
      <!-- <view class="desc-with-pics">
        ttcmk
        <view class="desc-pics">
          <image src="{{ img }}" mode="widthFix" wx:for-item="img" wx:for-index="key" wx:for="{{ img }}" wx:key="{{ img }}" class="desc-img"></image> -->
        <!-- <image src="http://lc-f6if1uws.cn-n1.lcfile.com/841a6c9193b7fdb4e4d6.png" mode="widthFix" class="desc-img"></image> -->
          

        <!-- </view> -->
      <!-- </view> -->
       <view class="info">
        <!-- <text class="sort">评论: ({{ document.comnum }})</text> -->
        <!-- <text class="sort">阅读: ({{ document.readNum }})</text> -->
        <!-- <text class="time">{{ document.publishedAt}}</text> -->
        <!-- <text class="time">{{ document.datetime}}</text> -->
      </view>
  </view>
</view>
<!-- <view class="read-more">
  {{ isend ? '无更多数据' : '加载中...' }}
</view> -->
</view>